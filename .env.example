# ─────────────────────────────────────────────────────────────────────────────
# EMERAUDE BUSINESS - Variables pour docker-compose
# Copier en .env et adapter
# ─────────────────────────────────────────────────────────────────────────────

# Base de données (aligné avec le service postgres)
POSTGRES_USER=emeraude
POSTGRES_PASSWORD=emeraude
POSTGRES_DB=emeraude

# NextAuth (obligatoire - doit correspondre à l'URL de l'app web)
# En local: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-in-production

# API séparée (optionnel - pour API et web sur des ports différents)
# Quand défini, le web (port 3000) proxy /api vers cette URL
# API: port 3001 | Web: port 3000
# API_URL=http://localhost:3001

# Grafana (optionnel)
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
